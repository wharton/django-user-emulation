============
Installation
============

At the command line::

    $ easy_install django-user-emulation

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-user-emulation
    $ pip install django-user-emulation
